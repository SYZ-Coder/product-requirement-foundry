# GitHub 项目简介

## 简短描述

把一句话产品需求转成兼容历史功能的 PRD 和可直接交给 AI 开发的 OpenSpec 交付包。

## 标准描述

`product-requirement-foundry` 是一个面向 AI 辅助需求生成的技能包，适合希望“让 AI 生成有用需求文档，但又不能脱离历史产品上下文”的团队。

它支持：

- `0->1` 绿地需求生成
- 面向已上线产品的 `1->n` 增量需求生成
- 从 PRD、wiki、OpenSpec、知识资产、API、埋点和企业文档中加载历史基线
- 可信度评分、证据链和评审门禁
- 面向 AI 开发的 OpenSpec proposal/design/spec/tasks/dev-context 交付

## 建议的 GitHub 仓库副标题

面向 AI 辅助产品研发的历史兼容 PRD 生成与 OpenSpec 交付技能包。

## 建议的 GitHub About 文案

从一句话产品需求生成 PRD、OpenSpec 产物和可信度报告，同时不忽略历史功能和线上行为。

## 建议的社交传播文案

Product Requirement Foundry 帮助团队把一句话需求转成基于历史事实的 PRD 和 AI-ready specs。它同时支持 `0->1` 与 `1->n`，内置基线扫描、可信度审查和 OpenSpec 交付链路。

## 建议的项目定位

这个项目适合被定义为：

- 一个 AI 辅助需求生成技能包
- 一个从产品意图到开发规格的桥接层
- 一个适合有历史产品资产团队使用的安全 `1->n` 需求流程
