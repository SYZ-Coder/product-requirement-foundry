# Glossary

- close connection: a user with strong relationship signals such as mutual follows, frequent interaction, or recent direct engagement
- feed detail page: the page that shows one feed item and its full comment thread
- comment ranking fallback: the existing ranking path used when stronger ordering signals are unavailable
- moderation visibility rules: the rules that determine whether a comment can appear at all
